# ClawControl frontend environment variables
# Copy to .env.local and adjust as needed.

# Backend API base URL (default: /api — proxied through Vite in dev)
VITE_API_BASE=/api

# WebSocket base URL (default: ws://current host — proxied through Vite in dev)
VITE_WS_BASE=ws://localhost:3000
